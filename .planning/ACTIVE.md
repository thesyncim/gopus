# Retired

This file is kept only as a migration stub.

The old planning-driven workflow has been retired in favor of:

- `program.md` for the autonomous experiment loop
- `results.tsv` for the keep/discard ledger
- `tools/autoresearch.sh` for preflight, baseline, and evaluation runs
