# Active Investigation

Last updated: 2026-03-10
Status: active
Active objective: close the fair example-bench gopus/libopus throughput gap without regressing fixture-backed parity or quality.
Current hypothesis: CELT decode still has the highest-leverage remaining wins on the example benches; after the stereo direct-output keep, the remaining gap is concentrated in shared synth/output and FFT/PVQ work rather than another stereo deemphasis asm retry.

Objective: preserve fixture-backed libopus 1.6.1 parity while pushing encoder/decoder hot paths toward libopus performance parity.

Older evidence entries were intentionally pruned on 2026-03-01 to keep this file operationally small.

## Current Snapshot

- Decoder parity remains stable across CELT/SILK/Hybrid lanes covered by the current fixture matrix.
- Encoder compliance summary is currently green under parity tier (`23 passed, 0 failed`).
- Recent merged hardening loops:
  - PR #259: tightened final CELT compliance override floor to `0.191 dB`.
  - PR #260: tightened `SILK-WB-60ms-mono-32k|impulse_train_v1` ratchet floors.
  - PR #261: tightened `SILK-WB-60ms-mono-32k|am_multisine_v1` ratchet floors.
- Required CI matrix is green on the latest merged loop (#275): linux parity/provenance/race/flake + macOS + windows + perf.
- Local broad gate note: `make verify-production` still hits the known local `tmp_check` cgo-disabled blocker; non-`tmp_check` packages pass.

## Remaining Gaps (Current)

- No blocking parity/compliance failures in the current guarded fixture set.
- Main remaining work is keeping those parity/quality guards green while closing the remaining encode/decode throughput gap versus libopus.

## Next 3 Actions

1. Keep taking exactness-guarded perf slices from the current encoder/decoder hotspots until the libopus gap closes materially.
2. Preserve fixture-backed stress/provenance coverage and only tighten ratchets when the perf loop changes fixture-backed behavior.
3. Keep running broad verification (`make verify-production`, `make bench-guard`) before merge-ready changes.

## Explicit Session Skips

- Do not re-debug SILK decoder correctness without new contradictory evidence.
- Do not re-debug resampler parity unless resampler code/fixtures change.
- Do not revisit NSQ constant-DC behavior unless fixtures indicate regression.

## Evidence Log (Newest First)

- 2026-03-10: Kept a stereo direct float32 decode path for the public CELT packet fast path. `celt/synthesis.go` now exposes a planar stereo synthesis helper so `DecodeFrameWithPacketStereoToFloat32()` can keep left/right planar through synthesis, `celt/postfilter.go` adds an exact planar stereo postfilter path, and `celt/decoder.go` now de-emphasizes directly from those planar buffers into the caller’s interleaved `[]float32` without building `scratchStereo` first. Focused exactness stayed green with `GOWORK=off go test ./celt -run '^(TestDecodeFrameWithPacketStereoToFloat32MatchesDecodeFrame|TestApplyPostfilterStereoPlanarMatchesInterleaved|TestDecodeSilenceFrameFastPathParityStereo)$' -count=1`, and a real regression during the first attempt (missing overlap-tail save in the planar synth helper) was fixed before broad validation. Broader validation is green with `GOWORK=off go test ./celt -count=1`, `GOWORK=off GOARCH=amd64 go test ./celt -count=1`, `GOPUS_TEST_TIER=parity GOWORK=off go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `GOWORK=off make bench-guard`. `GOWORK=off make verify-production` again hit the known local `tmp_check` cgo-disabled blocker first and then all runnable packages passed through `util` before `make` exited non-zero, matching the expected local pattern. Same-host decode evidence moved in the right direction after the corrected keep: `BenchmarkDecoderDecode_Stereo ~13655-13659 ns/op -> ~13344-13507 ns/op`, the fair speech decode example improved from `gopus avg 467.405986ms` to `avg 458.657028ms` (`libopus avg 417.809347ms`), and the fair stereo decode example improved from `gopus avg 2.442229444s` to `avg 2.356232208s` (`libopus avg 2.14280175s`). The new direct microbench `BenchmarkDecodeFrameWithPacketStereoToFloat32` lands around `~22593-22858 ns/op` on Apple M4 Max.
- 2026-03-10: Kept a dense libopus-style `U(n,k)` view in `celt/cwrs.go` so `cwrsiFast()` and `pvqUTableLookupFast()` can use direct row/column indexing instead of recomputing packed-table offsets in their inner loops. The packed `pvqUData` source of truth remains unchanged; `pvqUDense` is just a derived exact layout that mirrors `CELT_PVQ_U_ROW[row][col]` from libopus. Focused CWRS/CELT guards stayed green with `GOWORK=off go test ./celt -run '^(TestCWRS32|TestCWRS32AllIndices|TestCWRS32LargerDimensions|TestDecodePulses|TestDecodePulsesKnownVectors|TestDecodePulsesExhaustiveProperties|TestDecodeFrameWithPacketStereoToFloat32MatchesDecodeFrame)$' -count=1`, plus broader `GOWORK=off go test ./celt -count=1`, `GOWORK=off GOARCH=amd64 go test ./celt -count=1`, `GOWORK=off go test ./encoder -count=1`, `GOPUS_TEST_TIER=parity GOWORK=off go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `GOWORK=off make bench-guard`. Same-host microbenches improved on the fast-table shapes: `BenchmarkDecodePulses ~20.07-20.64 ns/op -> ~16.15-16.62 ns/op`, `BenchmarkCWRS32Decode/N16_K4 ~34.59-35.20 ns/op -> ~32.34-32.93 ns/op`, and `BenchmarkCWRS32Decode/N64_K2 ~104.4-106.3 ns/op -> ~87.62-87.65 ns/op` on the best current runs. The root decoder bench improved from the recent merged baseline `~8881-8955 ns/op` to current `~8548-8816 ns/op`, and the fair speech decode example improved from `gopus avg 468.741694ms` to `avg 463.510347ms` (`libopus avg 409.063458ms`). The same dense lookup also helped encoder-side covered table walks: `BenchmarkEncoderEncode` in `make bench-guard` improved from the recent `~46065 ns/op` guard sample to current `~45334 ns/op`, and the fair speech encode example on the current tip measured `gopus avg 2.296009096s` vs `libopus avg 1.867777528s`. `GOWORK=off make verify-production` again matched the known local blocker pattern: immediate `tmp_check` cgo-disabled failure, then all runnable packages through `silk` passed before the trailing `go test ./...` stage went silent.
- 2026-03-10: Corrected the `celt/kf_bfly5n1` rollout after `#287` CI exposed that the non-arm64 `N=1/mm=1` radix-5 specialization was not exact. The arm64 rolling-pointer keep remains intact, but the non-arm64 path now falls back to the exact general butterfly by delegating `kfBfly5N1Generic()` to `kfBfly5Inner(..., N=1, mm=1, ...)` and disabling `useKfBfly5N1()` off-arm64 until a proven exact specialization exists. Focused exactness is green again on both current and CI-relevant arches: `GOWORK=off go test ./celt -run '^TestKfBfly5N1MatchesReference$' -count=1` and `GOWORK=off GOARCH=amd64 go test ./celt -run '^TestKfBfly5N1MatchesReference$' -count=1` both passed. Broader validation also recovered with `GOWORK=off go test ./celt -count=1`, `GOWORK=off GOARCH=amd64 go test ./celt -count=1`, `GOPUS_TEST_TIER=parity GOWORK=off go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `GOWORK=off make bench-guard`. `GOWORK=off make verify-production` again matched the known local blocker pattern: immediate `tmp_check` cgo-disabled failure, then all runnable packages through `silk` passed before the trailing `go test ./...` stage went silent.
- 2026-03-10: Kept a stereo decode-history rewrite in `celt/postfilter.go` that reuses `DeinterleaveStereoInto()` for the common stereo `updatePLCDecodeHistory()` and `updatePostfilterHistory()` paths instead of maintaining separate scalar gather loops. This is exact bit-pattern movement only; it does not change comb filtering, PLC math, or sample order. Added exact regression and microbench coverage in `celt/postfilter_history_test.go`; focused guards passed with `GOWORK=off go test ./celt -run '^(TestStereoHistoryHelpersMatchLegacy|TestDecodeFrameWithPacketStereoToFloat32MatchesDecodeFrame)$' -count=1`, and broader validation stayed green with `GOWORK=off go test ./celt -count=1`, `GOPUS_TEST_TIER=parity GOWORK=off go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `GOWORK=off make bench-guard`. The direct helper benches on Apple M4 Max show the keep clearly: `BenchmarkUpdatePLCDecodeHistoryStereoCurrent ~425.4-451.8 ns/op` vs legacy `~699.2-701.6 ns/op`, and `BenchmarkUpdatePostfilterHistoryStereoCurrent ~268.2-269.3 ns/op` vs legacy `~509.3-519.7 ns/op`. Same-host root decode improved again to `BenchmarkDecoderDecode_CELT ~8881-8955 ns/op` while encode stayed flat-to-better (`BenchmarkEncoderEncode ~45385-45902 ns/op`). Sequential example benches improved on the same host after the earlier pulse-cache keep: speech decode `gopus avg 478.241458ms -> 465.687222ms`, stereo decode `gopus avg 2.447915146s -> 2.407769229s`. Fresh fair `opus_demo` decode comparison now lands at `speech 468.741694ms vs 409.250764ms` (~`14.5%` slower) and `stereo 2.417625708s vs 2.107915875s` (~`14.7%` slower), closing several more points from the prior ~`15-16%` gap.
- 2026-03-10: Kept a direct lookup path for static CELT pulse caches in `celt/pulse_cache.go`. `bitsToPulsesCachedFast()` now uses a precomputed exact `(cache start offset, bitsQ3) -> pulses` table for the shipped `cacheBits50` slices and falls back to the previous binary search for custom/non-static cache slices. Added exactness and bench coverage in `celt/pulse_cache_test.go`; focused guards passed with `GOWORK=off go test ./celt -run '^(TestBitsToPulsesCachedFastMatchesBinarySearch|TestBitsToPulsesCachedFastFallbackCustomSlice|TestDecodeFrameWithPacketStereoToFloat32MatchesDecodeFrame)$' -count=1`, and broader validation stayed green with `GOWORK=off go test ./celt -count=1`, `GOPUS_TEST_TIER=parity GOWORK=off go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `GOWORK=off make bench-guard`. Direct microbench on Apple M4 Max improved `BenchmarkBitsToPulsesCachedFast ~1.964-1.979 ns/op` versus legacy `BenchmarkBitsToPulsesCachedBinarySearch ~6.430-6.503 ns/op` while `BenchmarkDecodePulses{,Large}` stayed flat, confirming the keep is isolated to the cache search. Same-host root benches stayed favorable: `BenchmarkDecoderDecode_CELT ~8912-9016 ns/op` and `BenchmarkEncoderEncode ~45907-46598 ns/op`. The fair example decode rows improved materially on the same host: speech `gopus avg 507.533652ms -> 478.241458ms`, stereo `gopus avg 2.547169312s -> 2.447915146s`. Fresh fair `opus_demo` comparison puts the decode gap at `speech 479.113055ms vs 415.297639ms` (~`15.4%` slower) and `stereo 2.458207312s vs 2.11335675s` (~`16.3%` slower), down from the earlier ~`22%` range. During this loop I also verified the speech fixture is entirely stereo CELT 20 ms packets (`2475` packets, `0` SILK/Hybrid), so further decode work should stay on CELT PVQ/CWRS/IMDCT rather than SILK or mono-only helpers.
- 2026-03-10: Took another example-bench performance loop on top of the unmerged `celt/` claim without cutting a commit yet because the decode example acceptance bar is still mixed. Two current local keeps are in the worktree. First, `celt/haar1_arm64.s` now provides real arm64 fast paths for `haar1` stride `1` and `2`, with generic fallbacks retained in `celt/haar1_nonarm64.go`. Exactness guards passed (`GOWORK=off go test ./celt -run '^(TestHaar1SpecializedMatchesGeneric|TestHaar1StrideFastPathsMatchGenericExact|TestDecodeFrameWithPacketStereoToFloat32MatchesDecodeFrame)$' -count=1`) and direct benches favored the asm paths on Apple M4 Max: `BenchmarkHaar1Stride1Current ~15.8-16.1 ns/op` vs generic `~19.0-19.3 ns/op`, `BenchmarkHaar1Stride2Current ~30.1-30.4 ns/op` vs generic `~32.1-32.5 ns/op`. Second, plain stride-2 `deinterleaveHadamardInto` / `interleaveHadamardInto` in `celt/bands_quant.go` now route through `DeinterleaveStereoInto` / `InterleaveStereoInto`, picking up the existing exact arm64 stereo layout asm for the common non-Hadamard stride-2 case. Exact layout guards stayed green (`GOWORK=off go test ./celt -run '^(TestHadamardWorkIntoMatchesLegacy|TestHaar1SpecializedMatchesGeneric|TestHaar1StrideFastPathsMatchGenericExact|TestDecodeFrameWithPacketStereoToFloat32MatchesDecodeFrame)$' -count=1`), and the direct roundtrip bench improved materially: current `BenchmarkHadamardWorkRoundTripCurrentStride2 ~41.2-41.5 ns/op` vs legacy `~87.1-89.0 ns/op`. Same-host A/B against a clean detached `origin/master` worktree (`9cec334`) stayed favorable on root benches: baseline `BenchmarkDecoderDecode_CELT ~9285-9367 ns/op` vs current `~9140-9217 ns/op`, and baseline `BenchmarkEncoderEncode_Stereo ~79546-80199 ns/op` vs current `~78844-79711 ns/op`. Example benches remain mixed rather than merge-ready on the same host: speech encode improved slightly (`avg 2.452912271s -> 2.446105771s`), stereo encode improved slightly (`avg 10.232522958s -> 10.211683083s`), stereo decode improved (`avg 2.572074854s -> 2.547169312s`), but speech decode remained slightly slower/noisier on the current runs (`baseline avg 503.839041ms`, current `avg 507.533652ms`). In the same loop, two arm64 stereo deemphasis asm attempts were explicitly rejected and reverted: the first scalar-state version drifted by 1 ULP and ran at `~1563-1585 ns/op` vs generic `~1064-1066 ns/op`; a second exact FMA-form rewrite matched bits but still lost at `~1127-1139 ns/op` vs generic `~1064-1071 ns/op`. Next decode-side work should stay on mono/speech hotspots rather than more stereo-only kernels.
- 2026-03-10: Kept a common-case CELT radix-5 FFT specialization in `celt/kissfft32.go` plus new `celt/kf_bfly5n1.go` / `celt/kf_bfly5n1_arm64.s`. For the shipped CELT FFT sizes (`60/120/240/480`), the radix-5 stage always runs with `N=1` and `mm=1`, so `kfBfly5()` now dispatches that shape to `kfBfly5N1()` instead of the fully general double-loop `kfBfly5Inner()` kernel. The arm64 path uses rolling lane/twiddle pointers, while other arches fall back to an exact generic helper. Added direct exactness and stage microbench coverage in `celt/kf_bfly5_test.go`. Focused validation stayed green: `GOWORK=off go test ./celt -run '^(TestKfBfly5N1MatchesReference|TestKissFFT32_Accuracy|TestMDCTForwardOverlapDirectStageMatchesLegacyStagedPath)$' -count=1` passed. Broader CELT/parity/perf validation also stayed green: `GOWORK=off go test ./celt -count=1`, `GOPUS_TEST_TIER=parity GOWORK=off go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `GOWORK=off make bench-guard`. Local `GOWORK=off make verify-production` again hit the known `tmp_check` cgo-disabled blocker first, then the runnable packages through `silk` passed before the trailing `go test ./...` stage went silent, matching the existing local blocker pattern. Direct stage benches on Apple M4 Max showed the specialization helping the common smaller cases and staying near-neutral at the largest case: current `BenchmarkKfBfly5N1Current` vs current-path reference was about `58.0 ns` vs `60.0-61.5 ns` (`nfft60`), `120.4-120.6 ns` vs `122.9-124.8 ns` (`nfft120`), `238.9-242.1 ns` vs `243.2-245.3 ns` (`nfft240`), and `479.1-482.3 ns` vs `476.0-477.4 ns` (`nfft480`). Same-base end-to-end A/B against detached pre-change tip `aadcc09` favored the keep: baseline `BenchmarkEncoderEncode_Stereo ~79852-80737 ns/op` versus current `~78572-79160 ns/op`, and the speech harness (`GOWORK=off go run ./examples/bench-encode -sample speech -iters 3 -warmup 1 -mode gopus -bitrate 64000 -complexity 10 -batch 8`) improved from baseline `avg 1.997438347s` to current `avg 1.9721875s`.
- 2026-03-10: Kept an exact theta-RDO copy reduction in `celt/bands_quant.go`. The stereo theta-RDO path no longer snapshots the full first-trial `(x,y,lowbandOut)` result only to restore it later if that trial wins. Instead, trial 0 stays in the main output buffers, trial 1 runs in the existing scratch result buffers, and only the chosen trial is copied into the main band slices. This preserves the same two entropy-coder trials, distortion comparison, and final winning state while cutting redundant `copy()` traffic from the hot path. Focused validation stayed green: `GOWORK=off go test ./celt -run '^(TestQuantBandStereoPreparedLowbandMatchesLegacy|TestThetaRDOTrialRestoration|TestComputeThetaPhaseInversionDisable)$' -count=1` and `GOWORK=off go test ./celt -count=1` passed. Parity stayed green: `GOPUS_TEST_TIER=parity GOWORK=off go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`). Broad perf gate stayed green: `GOWORK=off make bench-guard` passed. Local `GOWORK=off make verify-production` again hit the known `tmp_check` cgo-disabled failure first, then the runnable packages through `silk` passed before the trailing `go test ./...` stage went silent, matching the existing local blocker pattern. Same-host A/B against detached merged-master `37371aa` favored the keep: current `BenchmarkEncoderEncode_Stereo ~79020-79272 ns/op` versus baseline `~81378-82701 ns/op`, and the fair speech harness (`GOWORK=off go run ./examples/bench-encode -sample speech -iters 3 -warmup 1 -mode gopus -bitrate 64000 -complexity 10 -batch 8`) improved from baseline `avg 1.969171486s` to current `avg 1.954683069s`.
- 2026-03-09: Kept two exact encoder hot-path cleanups after the `#284` merged baseline. First, `celt/cwrs.go` now uses direct `pvqUTableLookupFast()` in encoder-side CWRS lookup and covered `PVQ_V()` cases whenever the same static table coverage already guarantees every `U(n,k)` access the walk can make. Added exact guards plus a covered microbench in `celt/cwrs_lookup_test.go` and `celt/cwrs_test.go`. Second, `celt/stereo_encode.go` now routes `DeinterleaveStereoInto()` / `InterleaveStereoInto()` through default-on arm64 raw 64-bit move kernels in `celt/stereo_layout_arm64.s`, with exact bit-pattern coverage in `celt/stereo_layout_arm64_test.go`. Validation stayed green: `GOWORK=off go test ./celt -count=1`, `GOPUS_TEST_TIER=parity GOWORK=off go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `GOWORK=off make bench-guard`. `GOWORK=off make verify-production` again failed only on the known local `tmp_check` cgo-disabled blocker after the rest of the tree passed. Microbench evidence on Apple M4 Max favored both keeps: `BenchmarkICWRSLookupFastCovered ~42.25-42.50 ns/op` versus checked `~43.57-46.13 ns/op`; `BenchmarkInterleaveStereoInto ~296.1-298.7 ns/op` versus generic `~387.4-390.6 ns/op`; `BenchmarkDeinterleaveStereoInto ~253.4-255.7 ns/op` versus generic `~386.2-390.9 ns/op`. Same-base encoder A/B against detached `73a110c` also favored the combined keep: current `BenchmarkEncoderEncode_Stereo ~68419-68794 ns/op` versus baseline `~69335-70196 ns/op`, and the clean sequential speech harness (`GOWORK=off go run ./examples/bench-encode -sample speech -iters 3 -warmup 1 -mode gopus -bitrate 64000 -complexity 10 -batch 8`) improved from baseline `avg 1.673503333s` to current `avg 1.667545597s`.
- 2026-03-09: Kept an exact single-pass normalize+band-energy path in `celt/encode_frame.go` by routing the main CELT frame encoder through the existing `NormalizeBandsToArray{Mono,Stereo}WithBandE()` helpers instead of first computing linear band amplitudes in one pass and then recomputing them inside `NormalizeBandsToArrayInto()`. Added exact helper coverage in `celt/hybrid_encode_helpers_test.go` to prove both mono and stereo outputs match the old separate-pass sequence coefficient-for-coefficient and amplitude-for-amplitude. Validation stayed green: `go test ./celt -run '^(TestNormalizeBandsToArrayMonoWithBandEMatchesSeparatePasses|TestNormalizeBandsToArrayStereoWithBandEMatchesSeparatePasses)$' -count=1`, `go test ./celt -count=1`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `make bench-guard`. Same-host A/B against a clean detached `b6f8bd4` worktree favored the keep on both synthetic and real harnesses: current `BenchmarkEncoderEncode_Stereo ~73313-76166 ns/op` versus baseline `~76856-78082 ns/op`, and fair speech encode (`go run ./examples/bench-encode -sample speech -iters 3 -warmup 1 -mode gopus -bitrate 64000 -complexity 10 -batch 8`) improved from baseline `avg 1.904835167s` to current `avg 1.844498582s` (about `3.2%` faster).
- 2026-03-09: Kept an exact silence-gating helper cleanup in `celt/encode_frame.go`. The merged baseline still spent non-trivial time in `maxAbsSliceF64()` inside `computeSilenceFlag()`, and the old loop was paying `math.Abs` plus a branch per sample. Rewrote that helper to scan by absolute-value bit patterns with a 4-lane unrolled loop while preserving the legacy behavior for `NaN` (ignore), `Inf` (keep), and signed zero. Added direct legacy coverage plus a helper microbench in `celt/max_abs_test.go`. Focused validation stayed exact: `go test ./celt -run '^TestMaxAbsSliceF64MatchesLegacy$' -count=1` passed. Broader safety/perf checks also stayed green: `go test ./celt -count=1`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `make bench-guard`. The helper bench improved materially on Apple M4 Max: `mono480 ~162.9-169.7 ns/op` vs legacy `~395.8-402.7 ns/op`, `stereo960 ~636.7-668.1 ns/op` vs legacy `~1690-1868 ns/op`, and `stereo2880 ~1896-2044 ns/op` vs legacy `~5146-5330 ns/op`. Same-host fair speech encode A/B against clean merged baseline `c059ec2` favored the keep (`gopus avg 1.884561361s` current vs `1.903787722s` baseline, about `~1.0%` faster), while the synthetic root `BenchmarkEncoderEncode_Stereo` slice stayed effectively flat/noisy (`~78062-78500 ns/op` current vs `~77434-77954 ns/op` baseline), so treat this as a modest real-world encode win rather than a large synthetic-bench jump.
- 2026-03-09: Kept a second exact encoder-side MDCT cleanup in `celt/mdct_encode.go`. `mdctShortBlocksCore()` was still doing a redundant per-coefficient bounds check and a multiply-based `outIdx := b + i*shortBlocks` for every short-block store even though `mdctForwardShortOverlapScratchInto()` already proves `output` has the full frame span. Rewrote that interleave loop to a BCE-friendly running-index walk and added direct legacy coverage plus a stubbed helper bench in `celt/mdct_short_blocks_test.go`. Validation stayed green: `go test ./celt -run '^TestMDCTShortBlocksCoreMatchesLegacy$' -count=1`, `go test ./celt -count=1`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `make bench-guard`. The direct helper bench improved on Apple M4 Max from legacy `~538.7-554.6 ns/op` to current `~463.1-466.1 ns/op` (about `14-16%` faster). Same-base root A/B against a clean detached `8e90358` worktree stayed mildly favorable: current `BenchmarkEncoderEncode_Stereo ~73738-73888 ns/op` vs baseline `~74164-74706 ns/op`, roughly `0.6-1.2%` faster on this slice.
- 2026-03-09: Kept another exact transform-plumbing encoder win in `celt/bands_quant.go` and `celt/haar1.go`. The latest profile had `quantBandPreparedLowband` / `quantPartition` still paying meaningful time in generic transform staging: `haar1` fell back to the generic loop for stride `6/8/12`, and `deinterleaveHadamardInto` / `interleaveHadamardInto` were still hitting the generic row/column path for plain stride `12/16`. Added exact specializations for those missing cases plus focused guards in `celt/haar1_exact_test.go`, `celt/hadamard_work_test.go`, and extended `celt/haar1_bench_test.go`. Safety stayed green: `go test ./celt -run '^(TestHaar1SpecializedMatchesGeneric|TestHaar1Transform|TestHadamardWorkIntoMatchesLegacy)$' -count=1`, `go test ./celt -count=1`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `make bench-guard` all passed. The direct stride benches clearly favored the keep on Apple M4 Max: `BenchmarkHadamardWorkRoundTripCurrentStride12 ~62.41-65.02 ns/op` vs legacy `~108.1-110.0 ns/op`, and `BenchmarkHadamardWorkRoundTripCurrentStride16 ~55.34-58.16 ns/op` vs legacy `~94.16-100.3 ns/op`. Same-base root A/B against a clean detached `HEAD` worktree also stayed favorable: current `BenchmarkEncoderEncode_Stereo ~73436-73772 ns/op` vs baseline `~76069-76560 ns/op`, about `3.5-4%` faster. The short fair speech example stayed effectively flat (`avg 1.76948443s` current vs `1.769932792s` baseline), so treat this as a real root-bench win with example-level noise rather than a large harness-visible jump.
- 2026-03-08: Kept an exact stereo theta-RDO lowband-preparation reuse in `celt/bands_quant.go`. The encoder was rebuilding the lowband fold source twice per stereo theta-RDO band even though that lowband staging depends only on `(lowband, n, B, tfChange)` and is identical across the `theta_round=-1/+1` trials. Added `prepareQuantBandLowband()` plus prepared-lowband encode helpers so the x-channel lowband copy/haar/deinterleave path is materialized once and reused across both trials, with direct exact coverage in `celt/quant_band_prepared_lowband_test.go`. Focused safety checks passed: `go test ./celt -run '^TestQuantBandStereoPreparedLowbandMatchesStandard$' -count=1`, `go test ./celt -count=1`, and `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`). Same-host A/B against clean merged baseline `832c55c` favored keeping the change: root `BenchmarkEncoderEncode_Stereo` current `~80565-81544 ns/op` vs baseline `~82095-82267 ns/op`, and the short fair speech encode harness (`go run ./examples/bench-encode -sample speech -iters 3 -warmup 1 -mode gopus -bitrate 64000 -complexity 10`) improved from baseline `avg 1.967958458s` to current `avg 1.96057818s`.
- 2026-03-08: Kept two exact encoder-side cleanup wins and rejected one arm64 asm detour. In `encoder/encoder.go`, `applyDelayCompensation()` now copies the CELT delay tail and transition-prefill window directly from `delayBuffer`, and `updateDelayBufferInternal()` is collapsed to the actual invariant the old branches implemented: keep the last encoder-buffer window of raw input history. Added legacy-state coverage plus a direct microbench in `encoder/delay_compensation_test.go`; focused tests passed (`go test ./encoder -run '^(TestApplyDelayCompensationMatchesLegacyState|TestDelayCompensation_StreamDelay(Mono|Stereo)|TestPrepareCELTPCM_DelayCompensationGatedByLowDelay|TestCELTTransitionPrefillSnapshotsLibopusDelayHistoryWindow)$' -count=1`) and the helper bench improved materially versus the legacy shape (`Mono480 ~115 ns vs ~162 ns`, `Mono960 ~151-156 ns vs ~244-252 ns`, `Stereo480 ~218-222 ns vs ~322-333 ns`, `Stereo960 ~332-342 ns vs ~537-549 ns`, all `0 allocs/op`). In `celt/exp_rotation_coeffs.go`, `expRotation()` now uses a precomputed exact coefficient table for the production `(length,k,spread)` ranges and falls back to direct trig only outside that table; exact coefficient coverage passed (`go test ./celt -run '^(TestExpRotationCoefficientsMatchDirectComputation|TestRotationUnitLibopus)$' -count=1`) and full `go test ./celt -count=1` stayed green. Broad quality/perf checks also stayed green: `go test ./encoder -count=1`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), root stereo encode landed around `~81582-82852 ns/op`, and the fair speech encode harness improved from the recent `gopus avg ~1.987658666s` to `avg 1.954469531s` while libopus stayed around `avg 1.608260869s`. In the same loop, an arm64 default-on asm prototype for the stride-1 `expRotation1()` path was explicitly rejected and reverted: exact dispatch tests passed, but the existing bench harness regressed from the earlier Go path (`BenchmarkExpRotation1Stride1Len32 ~133.9-136.6 ns/op`, `Len64 ~269.8-274.3 ns/op`) to the asm attempt (`Len32 ~176.3-185.4 ns/op`, `Len64 ~355.3-360.4 ns/op`) on Apple M4 Max.
- 2026-03-08: Kept the stereo prefilter layout cleanup in `celt/prefilter.go` and `celt/stereo_encode.go`. `runPrefilter()` now uses `DeinterleaveStereoInto()` to stage the stereo frame into planar prefilter scratch and `InterleaveStereoInto()` to write the filtered planar outputs back, instead of open-coded per-channel scatter/gather loops. Added an exact helper roundtrip guard in `celt/stereo_layout_test.go`; `go test ./celt -run '^(TestStereoLayoutHelpersRoundTrip|TestRunPrefilterParityAgainstLibopusFixture)$' -count=1` passed, and `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` stayed green (`23 passed, 0 failed`). The root stereo encoder bench improved again on the same host (`BenchmarkEncoderEncode_Stereo ~84320-84870 ns/op` versus the prior Hadamard safe point around `~84892-85127 ns/op`), and the fair speech encode harness improved from `gopus avg 2.016978903s` to `avg 1.98866625s` while libopus stayed around `avg 1.637249736s`. In the same loop, an exact-order unroll attempt for `sumOfSquaresF64toF32()` was explicitly rejected and reverted because it lost badly on Apple M4 Max (`~87.5 ns/op` current vs legacy `~51.8-53.2 ns/op`) without improving the root bench.
- 2026-03-08: Kept the `quantBand` / `quantBandDecode` Hadamard work-buffer rewrite in `celt/bands_quant.go`. Instead of deinterleaving into scratch and immediately copying back into `x`, the hot path now deinterleaves directly into dedicated per-band work buffers (`bandEncodeScratch.quantWork` / `bandDecodeScratch.quantWork`) and only interleaves back into the original slice when resynthesis actually needs the reconstructed order. Added exactness/bench coverage in `celt/hadamard_work_test.go`. Focused guards stayed exact: `go test ./celt -run '^TestHadamardWorkIntoMatchesLegacy$' -count=1` and `go test ./celt -count=1` passed; parity stayed green with `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`). The initial direct roundtrip benchmark for a representative non-power-of-two stride dropped from legacy `~311.7-334.2 ns/op` with allocs to current `~77.7-79.9 ns/op`, and after specializing the hot `stride=3` / `stride=6` generic cases the direct path settled at `~63.3-63.7 ns/op` versus legacy generic `~92.3-92.7 ns/op` (`0 allocs/op`). Root encoder evidence moved in the right direction on the same host: `BenchmarkEncoderEncode_Stereo` before the work-buffer loop had been roughly `~83943-86471 ns/op`, and after the stride specialization it stabilized around `~84892-85127 ns/op`. The fair speech encode harness remained quality-stable but still well behind libopus (`gopus avg 2.016978903s` vs `libopus avg 1.665199583s` at `64 kbps`, `complexity 10`, `batch 8`), so this is a keep-but-not-enough step and the next loop still needs a larger encoder hotspot.
- 2026-03-08: Kept the current CELT perf batch after exactness and same-host benchmark checks. Three pieces landed together: (1) `celt/transient.go` now processes the stereo transient-analysis HP/pair-energy pass in one forward walk over interleaved PCM while preserving each channel's arithmetic order exactly; (2) `celt/bands_quant.go` fuses collapse-mask extraction with residual normalization in the common resynth PVQ path; and (3) arm64 `pvqExtractAbsSign` now defaults to the exact generic loop instead of the slower asm helper, while keeping the faster arm64 asm for `pvqSearchPulseLoop`, `celtPitchXcorr`, `pitchAutocorr5`, and IMDCT rotate helpers. New focused guards/benches: `celt/transient_bench_test.go`, `celt/bands_resynth_collapse_test.go`, `celt/pvq_dispatch_test.go`, `celt/arm64_helper_dispatch_test.go`, and `celt/imdct_rotate_dispatch_test.go`. Validation passed: `go test ./celt -count=1`, `go test ./encoder -count=1`, and `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`). Same-host benches favored the kept paths: `BenchmarkTransientAnalysisCurrentStereo ~5294-5424 ns/op` vs legacy `~8051-8184 ns/op`, mono `~3796-4037 ns/op` vs `~4452-4586 ns/op`, `BenchmarkNormalizeResidualIntoAndCollapseCurrent ~23.37-23.43 ns/op` vs legacy `~31.02-32.04 ns/op`, and `BenchmarkPVQExtractAbsSignCurrent ~13.25-13.44 ns/op` after demoting the slower arm64 asm path. Root encoder benches stayed favorable on the same worktree (`BenchmarkEncoderEncode ~47122/47194/49397 ns/op`; `BenchmarkEncoderEncode_Stereo ~83943-86471 ns/op`), but the fair speech encode harness still trails libopus materially (`gopus avg 2.019779111s` vs `libopus avg 1.661602875s` at `64 kbps`, `complexity 10`, `batch 8`), so the next loops should stay on encoder/CELT hotspots rather than treating this as parity reached.
- 2026-03-08: PR #278 (`perf(celt): runtime-dispatch amd64 asm`) initially failed `test-linux-parity` on Linux amd64 in `TestAMD64DispatchMatchesGeneric` because `expRotation1Stride2AVXFMA` changed the final float64 result very slightly versus the generic helper (`expRotation1Stride2 mismatch` in CI). Keep the runtime-dispatch branch, but stop routing `expRotation1Stride2()` through the AVX/FMA kernel so the default-on amd64 dispatch only uses kernels that satisfy the existing exactness guard. Local validation after the fix: `go test ./celt -count=1`, `make bench-guard`, `GOOS=linux GOARCH=amd64 go test -c ./celt`, and `GOOS=linux GOARCH=amd64 go test -c .` all passed; PR #278 is re-running CI for the real Linux amd64 parity confirmation.
- 2026-03-08: Replaced CELT amd64 `amd64.v3` source selection with in-repo runtime CPU dispatch so the shipped binary carries both the generic and AVX2/FMA asm paths with no user build tags. Added `internal/cpufeat` for amd64 AVX2/FMA probing and Darwin arm64 feature inventory, replaced `celt/amd64_dispatch_v1.go` / `celt/amd64_dispatch_v3.go` with a single runtime-selected `celt/amd64_dispatch.go`, and retagged the AVX/FMA amd64 assembly files from `amd64.v3` to plain `amd64`. Focused validation passed: `go test ./celt -run 'Test(CeltInnerProd|DualInnerProd|CeltPitchXcorr|PrefilterPitchXcorr)' -count=1`, `GOOS=linux GOARCH=amd64 go test -c ./celt`, and `GOOS=linux GOARCH=amd64 go test -c .`. Same-host A/B against a clean detached `master` worktree (`563d1a6`) showed no arm64 regression from the refactor itself: current `BenchmarkEncoderEncode ~45942-46123 ns/op` vs master `~46203-46617 ns/op`, and current `BenchmarkEncoderEncode_Stereo ~83452-84451 ns/op` vs master `~84235-85047 ns/op`. The attempted new arm64 `celtInnerProd` asm path was explicitly rejected and reverted in the same loop because it lost to the existing Go path on this host.
- 2026-03-08: Specialized `encoder/gemmAccumF32` for the only production row counts used by the tonality MLP (`2`, `24`, `32`) so those paths accumulate into fixed local registers instead of repeatedly loading/storing the output slice inside the inner loop. Added an exact generic-reference guard in `encoder/analysis_mlp_exact_test.go`; `go test ./encoder -run '^TestGemmAccumF32MatchesGenericReference$' -count=1` passed. The MLP microbenches improved materially on the same host: `BenchmarkGemmAccumF32Rows24 ~194.1-196.6 ns/op -> ~89.7-90.6 ns/op`, `BenchmarkGemmAccumF32Rows32 ~197.3-198.6 ns/op -> ~95.8-97.6 ns/op`, and `BenchmarkAnalysisGRU ~1145-1183 ns/op -> ~575-586 ns/op` (about `50%` faster). That translated into encoder-side wins without quality drift: `BenchmarkTonalityAnalysis48kMono ~7114-7162 ns/op -> ~6419-6591 ns/op`, `BenchmarkTonalityAnalysis48kStereo ~7409-7465 ns/op -> ~6731-6857 ns/op`, same-base root `BenchmarkEncoderEncode ~47006-47292 ns/op` at safe-point `3c00f78` vs current `~46732-47052 ns/op`, and the fair speech encode example (`go run ./examples/bench-encode -sample speech -iters 3 -warmup 1 -mode both -bitrate 64000 -complexity 10 -batch 8`) improved from baseline `avg 2.00178525s` to current `avg 1.992770028s` while libopus remained around `avg 1.44119325s -> 1.451478985s` run-to-run. Safety checks stayed green: `go test ./encoder -count=1`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `make bench-guard`.
- 2026-03-08: Replaced the temporary amd64 CELT runtime CPU probing with stdlib-style build selection and removed the last user-facing asm opt-in path. The AVX/FMA CELT kernels now live behind `amd64.v3` source selection (`celt/amd64_dispatch_v3.go` plus `*_amd64.s` build tags), older x86 uses exact generic helpers via `celt/amd64_dispatch_v1.go`, and arm64 remains default-on because the shipped arm64 kernels only use baseline ISA. Removed `golang.org/x/sys/cpu` from the module graph, deleted `celt/amd64_dispatch.go`, and updated `celt/amd64_dispatch_test.go` to compare the active path directly against the generic helpers. Validation passed on the native host and the old-hardware amd64 slice: `go test ./celt -count=1`, `GOARCH=amd64 GOAMD64=v1 go test ./celt -run '^TestAMD64DispatchMatchesGeneric$' -count=1`, `GOARCH=amd64 GOAMD64=v1 go test ./celt -count=1`, `GOARCH=amd64 GOAMD64=v3 go test ./celt -run '^$' -c -o /tmp/celt_amd64v3.test`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `make bench-guard` all passed.
- 2026-03-08: Removed the dead experimental `sum_sq` / `spread_count` asm paths instead of carrying them behind opt-in tags. Audit results were not good enough to justify default enablement: the arm64 `spread_count` kernel had a compare-direction correctness bug when first enabled for testing, and even after fixing that locally it still lost to the generic Go loop on the current host (`BenchmarkSpreadCountThresholdsLegacy ~16.09-17.08 ns/op` vs asm `~24.26-24.42 ns/op`). The `sum_sq` asm path was also not exact: the vector lane-accumulation shape changed float32 accumulation order and failed a direct legacy check at small sizes. Removed the opt-in asm/dispatch files for both helpers, kept the generic implementations as the only shipped path, and added a focused regression guard in `celt/sum_spread_fastpath_test.go`. Safety checks stayed green after the cleanup: `go test ./celt -count=1`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `make bench-guard` all passed.
- 2026-03-08: Kept the mono 48 kHz analysis down2 helper unroll in `encoder/analysis.go` after re-testing it on top of the merged amd64 assembly-safety master. `silkResamplerDown2HP()` and `silkResamplerDown2HPStereo()` now process two output samples per loop while preserving the exact arithmetic order and state transitions of the legacy scalar loop. Added an exact legacy guard for the mono helper in `encoder/analysis_test.go` and a focused microbench in `encoder/analysis_resampler_bench_test.go`. Focused validation passed: `go test ./encoder -run '^(TestSilkResamplerDown2HPMatchesLegacy|TestSilkResamplerDown2HPStereoMatchesDownmixThenResample)$' -count=1`. Same-base arm64 A/B against `origin/master` favored keeping the change: `BenchmarkSilkResamplerDown2HPLegacy ~947.7-978.3 ns/op` vs `BenchmarkSilkResamplerDown2HPCurrent ~874.1-895.4 ns/op`, `BenchmarkTonalityAnalysis48kMono ~7250-7284 ns/op` baseline vs `~7158-7187 ns/op` current, and root `BenchmarkEncoderEncode ~47245-47784 ns/op` baseline vs `~46996-47321 ns/op` current. This is a modest encoder win, but it is exact and end-to-end favorable rather than just a microbench gain.
- 2026-03-08: Tightened the 48 kHz encoder tonality-analysis staging path in `encoder/analysis.go` by adding a stereo-only fused downmix+down2 helper (`silkResamplerDown2HPStereo`) and routing the hot stereo analysis path through it, while intentionally keeping mono on the old staged path after same-base A/B showed the mono fused variant regressed on Apple M4 Max. Added a stereo helper equivalence guard in `encoder/analysis_test.go` and revalidated the full libopus analysis fixture (`go test ./encoder -run '^(TestSilkResamplerDown2HPStereoMatchesDownmixThenResample|TestAnalysisTraceFixtureParityWithLibopus)$' -count=1 -v` passed). Same-base stash A/B on the current merged master showed 48 kHz tonality stereo improving from baseline `~7649-7697 ns/op` to `~7353-7362 ns/op` (~`4%` faster) while mono stayed effectively flat after narrowing the change (`baseline ~7344-7371 ns/op`, current `~7354-7384 ns/op`). The root stereo encoder bench stayed slightly favorable on the same base (`BenchmarkEncoderEncode_Stereo` baseline `~87530-87876 ns/op`, current `~87201-87722 ns/op`, about `0.3%` better on average). Broader safety checks stayed green: `go test ./encoder -count=1`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `make bench-guard` passed. `make verify-production` again failed only on the known local `tmp_check` cgo-disabled blocker after all other packages, including `testvectors`, passed.
- 2026-03-08: Tightened `removeDoubling()` in `celt/prefilter.go` by aligning its `yy_lookup` scratch with libopus float-width semantics. The old Go path stored the running `yy` values in `[]float64` scratch and immediately converted them back to `float32` on use; the new path keeps `encoderScratch.prefilterYYLookup` as `[]float32`, eliminating the extra widen/narrow churn without changing the decision logic. Added an exact legacy-reference guard plus a microbench in `celt/remove_doubling_test.go`; focused validation passed (`go test ./celt -run '^TestRemoveDoublingMatchesLegacyYYLookup$' -bench '^(BenchmarkRemoveDoublingCurrent|BenchmarkRemoveDoublingLegacy)$' -count=3 -cpu=1`). The microbench improved from legacy `~2496-2601 ns/op` to current `~2265-2300 ns/op` (~`9-13%` faster). The gain held up end-to-end on the same host versus baseline worktree `2242402`: root `BenchmarkEncoderEncode` improved from `~47325-47750 ns/op` to `~47231-47336 ns/op`, and the fair speech encode example (`go run ./examples/bench-encode -sample speech -iters 3 -warmup 1 -mode both -bitrate 64000 -complexity 10 -batch 8`) improved from `avg 2.00886493s` to `avg 1.991523458s` while libopus measured `avg 1.613990639s` on the current run. Quality/perf guards stayed green: `go test ./celt -count=1`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `make bench-guard`.
- 2026-03-08: Kept a small but exact CELT long-block IMDCT decode cleanup in `celt/mdct.go`: `imdctOverlapWithPrevScratchF32()` now post-rotates directly into the overlap/output scratch region instead of writing to a separate float32 `buf` and copying it into place before TDAC windowing. Added a bit-exact regression guard in `celt/imdct_overlap_f32_test.go`; focused checks passed (`go test ./celt -run '^(TestIMDCTOverlapWithPrevScratchF32MatchesLegacyBufferCopy|TestDecodeFrameWithPacketStereoToFloat32MatchesDecodeFrame|TestMDCT.*|TestLibopus.*MDCT.*|TestDecodeFrame_SampleCount|TestDecodeFrame_SampleCount_Stereo|TestDecodeFrame_InvalidFrameSizeRejected)$' -count=1`). Full CELT tests stayed green (`go test ./celt -count=1`), and `make bench-guard` passed. Same-host A/B against safe-point worktree `3b416d0` stayed slightly favorable on decode: `BenchmarkDecoderDecode_CELT` baseline `~9168-9312 ns/op` vs current `~9074-9280 ns/op`, and the fair speech decode example (`go run ./examples/bench-decode -sample speech -iters 3 -warmup 1 -mode both -batch 8`) improved from baseline `avg 496.15843ms` to current `avg 494.475055ms` while libopus remained around `~379-408ms` depending run. This is an incremental decode-side win, not a large loop.
- 2026-03-08: Kept the CELT MDCT direct-stage fast path in `celt/mdct_encode.go` after validating it against both the legacy staged implementation and end-to-end encoder quality/perf guards. The change avoids materializing the intermediate folded `f[]` buffer on the normal direct `kissCpx` path and writes the pre-twiddled values straight into the bit-reversed FFT scratch. Added an exact staged-reference guard in `celt/mdct_stage_test.go`; focused MDCT checks passed (`go test ./celt -run '^(TestMDCTForwardOverlapDirectStageMatchesLegacyStagedPath|TestMDCT.*|TestLibopus.*MDCT.*|TestMDCTForward.*|TestMDCTShort.*)$' -count=1`). The direct-stage test is bit-exact on the exercised CELT sizes (`120/240/480/960`). Isolated MDCT forward A/B against the last pushed baseline worktree `e50002a` improved materially on the same host: `frameSize=120 ~612.0-616.4 ns/op -> ~434.5-453.0 ns/op`, `240 ~1083-1089 ns/op -> ~790.2-801.0 ns/op`, `480 ~2021-2055 ns/op -> ~1515-1526 ns/op`, `960 ~3947-4004 ns/op -> ~3045-3075 ns/op` (about `22-29%` faster depending size). End-to-end encoder perf also held up: root `BenchmarkEncoderEncode` moved from baseline `~48822-49219 ns/op` to current `~48129-49205 ns/op`, and the fair speech encode example (`go run ./examples/bench-encode -sample speech -iters 3 -warmup 1 -mode both -bitrate 64000 -complexity 10 -batch 8`) improved from baseline `avg 2.034969125s` to `avg 2.025911069s` while libopus measured `avg 1.663252444s`. Quality stayed green: `go test ./celt -count=1` passed and `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` passed (`23 passed, 0 failed`).
- 2026-03-08: Tightened the shared CELT zero-pulse resynthesis path used by `quantPartition()` / `quantPartitionDecode()` without changing behavior. Added `seededZeroPulseResynth()` in `celt/bands_quant.go` to fuse zero-pulse noise/fold generation with the exact same quartet-ordered energy accumulation already used by `renormalizeVector`, so zero-pulse bands no longer pay a separate full energy scan before scaling. Added exact legacy-equivalence coverage plus a direct helper microbench in `celt/bands_zero_pulse_test.go`; focused checks passed (`go test ./celt -run '^(TestSeededZeroPulseResynthMatchesLegacy|TestSeededZeroPulseResynthFallback|TestFindBestPitchInRangesMatchesFullSweep|TestDecodeFrameWithPacketStereoToFloat32MatchesDecodeFrame)$' -count=1`). The helper microbench improved materially versus the legacy path: noise `~19.55-19.78 ns/op` vs `~76.99-77.83 ns/op`, fold `~24.30-24.49 ns/op` vs `~152.5-153.8 ns/op`. End-to-end decoder benefit held up in A/B against the last pushed commit (`2bf74af`) using the same host and benchmark slice: baseline `BenchmarkDecoderDecode_CELT ~10341-10554 ns/op`, current `~9086-9173 ns/op` (about `12%` faster), while encoder stayed effectively flat (`baseline ~48108-48507 ns/op`, current `~47950-48584 ns/op`). The fair speech decode example also improved versus the baseline worktree: baseline `avg 514.151069ms`, current `avg 495.682292ms` at `batch 8` (~`3.6%` faster), with libopus still around `~371-404ms` depending run. Validation stayed green: `go test ./celt -count=1`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`), and `make bench-guard` passed. `make verify-production` again failed only on the known local `tmp_check` cgo-disabled blocker after the rest of the tree (including `testvectors`) passed.
- 2026-03-08: Fixed the long-file libopus benchmark “hang” in the example harness. Root cause was not `opus_demo`; it was `internal/benchutil.WriteRepeatedRawFloat32()` doing one 4-byte `Write` syscall per sample while preparing the repeated raw float32 input file for `opus_demo`. On the `ehren-paper_lights-96.opus` stereo sample (`228.11s`, `batch 8`) that means hundreds of millions of tiny writes before the child encoder even starts, which looks like libopus is stuck after the gopus section prints. Reworked `internal/benchutil/opus_demo.go` to use `bufio.Writer` plus chunked sample packing for raw float32 input and buffered writes for `.bit` streams, and added unit coverage in `internal/benchutil/opus_demo_test.go` (`go test ./internal/benchutil ./examples/bench-encode ./examples/bench-decode -count=1` passed). Also added explicit stage prints in the encode/decode examples before preparing libopus inputs and before running `opus_demo` so long runs no longer sit silent.
- 2026-03-08: Tightened the CELT pitch-search second stage without changing candidate semantics by replacing the full second-pass `findBestPitch()` sweep with `findBestPitchInRanges()` on the exact union of the two `+-2` lag windows that can have non-zero second-stage correlations, while still advancing the same sliding `Syy` state through skipped indices. Mirrored the same helper in the PLC pitch search to keep the code paths aligned. Added a randomized equivalence guard in `celt/pitch_search_test.go` (`go test ./celt -run '^(TestFindBestPitchInRangesMatchesFullSweep|TestDecodeFrameWithPacketStereoToFloat32MatchesDecodeFrame)$' -count=1` passed). Full CELT package tests stayed green (`go test ./celt -count=1`), `make bench-guard` still passed, and `make verify-production` again failed only on the known local `tmp_check` cgo-disabled blocker while the rest of the tree passed. Isolated pitch-search microbench improved from prior `BenchmarkPitchSearchCurrent ~3260-3309 ns/op` to `~3163-3223 ns/op` (~`3%` faster) while legacy stayed at `~4116-4141 ns/op`; whole-root encode benches remained thermally noisy on this host but stayed within the same zero-allocation guardrails.
- 2026-03-07: Reworked the end-to-end perf comparison harness to make libopus measurements fair and source-pinned, then landed two low-risk hot-path reductions on the current tree. Added `internal/benchutil/opus_demo.go` and switched `examples/bench-encode` / `examples/bench-decode` from ffmpeg and per-iteration process startup bias to batched `tmp_check/opus-1.6.1/opus_demo` runs over repeated raw float32 input / repeated `.bit` streams. On the codec side, the public float32 encoder path now exposes the original frame directly to analysis/VAD and skips the no-op 24-bit quantize round-trip, and CELT decode now has a direct `DecodeFrameWithPacketStereoToFloat32` path that writes final de-emphasized output straight into the caller buffer for the packetChannels==decoder channels case. Quality guard for the new decoder path is exact on the exercised CELT lane: `go test ./celt -run '^(TestDecodeFrameWithPacketStereoToFloat32MatchesDecodeFrame|TestDecodeFrame_SampleCount|TestDecodeFrame_SampleCount_Stereo|TestDecodeFrame_InvalidFrameSizeRejected)$' -count=1` passed. Example fairness/perf evidence on the speech fixture with batched libopus calls: `go run ./examples/bench-encode -sample speech -iters 3 -warmup 1 -mode both -bitrate 64000 -complexity 10 -batch 8` improved gopus from prior `best 1.963806167s / avg 1.974576042s` to `best 1.84371325s / avg 1.867150694s` (~`5.8%` avg faster) while libopus measured `best 1.448001042s / avg 1.467026653s`; `go run ./examples/bench-decode -sample speech -iters 3 -warmup 1 -mode both -batch 8` improved gopus from prior `best 502.716417ms / avg 506.869819ms` to `best 465.246625ms / avg 479.439652ms` (~`5.4%` avg faster) while libopus measured `best 386.752375ms / avg 397.941097ms`. Focused examples/docs stayed green (`go test ./examples/bench-encode ./examples/bench-decode -count=1`), `make bench-guard` passed, and `make verify-production` failed only on the known local `tmp_check` cgo-disabled blocker while all other packages passed.
- 2026-03-07: Closed the first end-to-end perf loop that actually clears the current speech-example target by removing the `unext()` row-recurrence hot path from CWRS encode whenever the static PVQ `U(n,k)` table already covers every needed row lookup. Added `icwrsLookupFast()` in `celt/cwrs.go` and routed both `EncodePulsesScratch` and `encodePulsesFast` through it before allocating/stepping the dynamic `u` row. Focused CWRS correctness stayed green: `GOMAXPROCS=1 go test ./celt -run '^(Test.*CWRS.*|Test.*Pulses.*|TestPVQ_V.*|TestNCWRS.*)$' -count=1`. Full CELT and encoder analysis slices also stayed green: `GOMAXPROCS=1 go test ./celt -count=1` and `GOMAXPROCS=1 go test ./encoder -run '^(TestAnalysis|TestRunAnalysis|TestTonalityAnalysis|TestUpdateOpusVADReusesFreshAnalysis|TestAnalysisTraceFixtureParityWithLibopus)$' -count=1`. Direct CWRS encode microbench improved by roughly 2x across representative CELT shapes versus a throwaway baseline worktree with the same surviving perf stack but without the new fast path: `BenchmarkCWRS32Encode` moved from baseline `N8_K4 ~24.3-24.6 ns`, `N16_K4 ~49.1-50.0 ns`, `N32_K3 ~91.7-92.0 ns`, `N64_K2 ~166-167 ns` to current `~12.6-13.0 ns`, `~22.1 ns`, `~40.6-41.6 ns`, `~81-82 ns`; `BenchmarkCWRS32RoundTrip/N16_K4` improved from `~80.2-81.0 ns` to `~55.0-56.5 ns`. Root encode bench also moved materially: `go test . -run '^$' -bench '^(BenchmarkEncoderEncode$|BenchmarkEncoderEncodeInt16$)' -count=3 -cpu=1` measured current `BenchmarkEncoderEncode ~43.8-44.1 us/op` and `BenchmarkEncoderEncodeInt16 ~44.1-44.4 us/op`. The speech example finally crossed the target versus clean `HEAD` baseline: clean `HEAD` worktree measured `best 255.378875ms / avg 257.276052ms`, while the current candidate measured `best 228.690125ms / avg 229.578783ms` on the first run and `best 229.583250ms / avg 232.349939ms` on the second run, i.e. about `10.5%` faster on best-case and `~9.7-10.8%` faster on average. Broad gates: `make bench-guard` passed; `make verify-production` still failed only on the known local `tmp_check` cgo-disabled blocker. Parity guard stayed green: `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`).
- 2026-03-06: Returned to end-to-end profiling after the rejected float32 prefilter scratch prototype. Fresh `BenchmarkEncoderEncode` CPU profile on Apple M4 Max (`go test . -run '^$' -bench '^(BenchmarkEncoderEncode$|BenchmarkEncoderEncodeInt16$)' -count=1 -benchtime=3s -cpu=1 -cpuprofile /tmp/gopus-encode-20260306.pprof`) showed `celt.(*Encoder).transientAnalysisScratch` back at the top of the remaining flat-cost list (`0.52s flat`, `7.10%`), ahead of `prefilterPitchXcorr` (`0.42s`, `5.74%`). Reworked `celt/transient.go` to fuse the HP filter and forward pair-energy pass, eliminating the transient sample scratch round-trip and removing unused `transientTmp` scratch from `celt/encoder.go`. Focused transient guards stayed green: `go test ./celt -run '^(TestTransientAnalysisTfEstimate|TestWeakTransientMode|TestTransientAnalysisWithState|TestStereoTransientDetection)$' -count=1` and then full `go test ./celt -count=1`. Added direct A/B hotspot coverage in `celt/transient_bench_test.go`; `go test ./celt -run '^$' -bench '^(BenchmarkTransientAnalysis(Current|Legacy))$' -count=5 -cpu=1` measured current `~6.21-6.34 us/op` versus legacy `~6.94-7.26 us/op` (~`8-13%` faster). Post-change source profile reduced `transientAnalysisScratch` to `0.39s flat`, `0.55s cum` (~`25%` flat reduction on the sampled run). Repeated root encoder benches remained in the same noisy `~51-53 us/op` band (`go test . -run '^$' -bench '^(BenchmarkEncoderEncode$|BenchmarkEncoderEncodeInt16$)' -count=5 -cpu=1`), so this is currently a confirmed hotspot-local win with no clear end-to-end regression, but broad perf gating still needs a full follow-up loop.
- 2026-03-05: Prototyped the broader float32 quarter-rate prefilter scratch path that the earlier asm investigation had left as the next plausible win: added dedicated float32 quarter-rate decimation scratch plus a celt-local ARM64 float32 xcorr helper, validated correctness on focused randomized xcorr tests and `TestRunPrefilterParityAgainstLibopusFixture`, then benchmarked it against the restored baseline. The result was a clear regression on Apple M4 Max, so the production code was reverted completely. Focused perf evidence: `go test ./celt -run '^$' -bench '^(BenchmarkPrefilterPitchXcorr|BenchmarkPrefilterPitchXcorrFloat|BenchmarkPitchSearch(Current|Legacy))$' -count=5 -cpu=1` measured baseline `BenchmarkPrefilterPitchXcorr ~3.92-3.96 us/op`, new float32 helper `BenchmarkPrefilterPitchXcorrFloat ~7.55-7.75 us/op`, and candidate `BenchmarkPitchSearchCurrent ~5.28-5.36 us/op` versus restored baseline `~3.72-3.76 us/op` (legacy `~4.07-4.08 us/op`). Same-session top-level encoder probe also regressed under the candidate and improved again after revert (`go test . -run '^$' -bench '^(BenchmarkEncoderEncode$|BenchmarkEncoderEncodeInt16$)' -count=1 -cpu=1`: candidate `~58.6/59.2 us/op`, restored `~55.7/56.3 us/op`). Conclusion: the direct float32 quarter-rate scratch swap is not a viable path on this host with the current helper shape; future perf work should target a materially different kernel/layout or another hotspot entirely.
- 2026-03-05: Investigated ARM64 `celt/prefilter_xcorr_arm64.s` because `BenchmarkEncoderEncode` CPU profile on Apple M4 Max showed `celt.prefilterPitchXcorr` as the hottest single asm kernel (`0.37s flat`, `7.23%`). Three source-preserving asm variants were tested and rejected after focused correctness + microbench loops. A libopus-style shifted-window kernel with `VEXT`-built windows was made correct after diagnosing Go ARM64 `VEXT` source-order reversal, but benchmarked much slower (`~6.50-6.76 us/op` vs baseline `~3.76-3.81 us/op` in `go test ./celt -run '^$' -bench '^BenchmarkPrefilterPitchXcorr$' -count=5 -cpu=1`). A dual-accumulator 8-wide variant also regressed (`~4.09-4.14 us/op`). A multi-register `ld1` load-pair variant was effectively neutral-to-slightly-worse (`~3.80-3.83 us/op`). The production kernel was restored exactly to baseline. Correctness checks passed on each retained baseline restoration: `go test ./celt -run '^(TestPrefilterPitchXcorr|TestPrefilterPitchXcorrEdge)$' -count=1`. Conclusion for this host: the current float64->float32 4-stream kernel is already near the local optimum; the next credible win likely requires a broader float32 quarter-rate prefilter scratch path instead of more asm reshaping.
- 2026-03-04: Optimized `findBestPitch` in `celt/prefilter.go` by adding bounds-check hints and skipping float64->float32 conversion work when `xcorr[i] <= 0` (`if xv := xcorr[i]; xv > 0 { ... }`). This targets the second pitch-search pass where most `xcorr` entries are zero after sparse window fill. Decision logic and Syy updates remain unchanged. Quality/parity remained green: `go test ./celt -count=1`; `go test ./encoder -count=1`; `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`); full runnable sweep passed (`go list -e -f '{{if not .Error}}{{.ImportPath}}{{end}}' ./... | rg -v '/tmp_check$' | xargs go test -count=1`). Perf guard stayed green (`make bench-guard`). Bench-binary stash A/B (`go run ./examples/bench-encode -sample speech -iters 20 -warmup 3 -mode gopus -bitrate 64000 -complexity 10`) improved from baseline `best 260.523958ms`, `avg 263.289118ms` to candidate `best 258.820209ms`, `avg 261.284043ms` (~`0.65%` best, `0.76%` avg faster).
- 2026-03-04: Optimized transient harmonic-mean indexing path in `celt/transient.go` by hoisting normalization directly into a float32 `normE` scalar and removing per-iteration float64 conversions in the `transientInvTable` loop (`id := int(normE * (energy[i] + epsF32))`). This keeps non-negative truncation semantics and threshold logic unchanged while reducing loop overhead. Quality/parity remained green: `go test ./celt -count=1`; `go test ./encoder -count=1`; `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`); full runnable sweep passed (`go list -e -f '{{if not .Error}}{{.ImportPath}}{{end}}' ./... | rg -v '/tmp_check$' | xargs go test -count=1`). Perf guard stayed green (`make bench-guard`). Bench-binary stash A/B (`go run ./examples/bench-encode -sample speech -iters 20 -warmup 3 -mode gopus -bitrate 64000 -complexity 10`) improved from baseline `best 260.885500ms`, `avg 263.234895ms` to candidate `best 257.531416ms`, `avg 262.346818ms` (~`1.29%` best, `0.34%` avg faster).
- 2026-03-04: Optimized analysis MLP hot path in `encoder/analysis_mlp.go` by unrolling `gemmAccumF32` row accumulation 4-way with a scalar tail (arithmetic order per output lane preserved). Quality/parity remained green: `go test ./celt -count=1`; `go test ./encoder -count=1`; `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`); full runnable sweep passed (`go list -e -f '{{if not .Error}}{{.ImportPath}}{{end}}' ./... | rg -v '/tmp_check$' | xargs go test -count=1`). Perf guard stayed green (`make bench-guard`). Bench-binary stash A/B (`go run ./examples/bench-encode -sample speech -iters 20 -warmup 3 -mode gopus -bitrate 64000 -complexity 10`) improved from baseline `best 261.709625ms`, `avg 263.283785ms` to candidate `best 257.730042ms`, `avg 260.000585ms` (~`1.52%` best, `1.25%` avg faster).
- 2026-03-04: Optimized ARM64 `toneLPCCorr` in `celt/tone_lpc_corr_arm64.s` by switching to pointer-walk delayed streams (`x+delay`, `x+delay2`) instead of recomputing delayed addresses every iteration. Accumulation order and float32 math are unchanged. Quality/parity remained green: `go test ./celt -count=1`; `go test ./encoder -run 'Test(Analysis|RunAnalysis|TonalityAnalysis|UpdateOpusVADReusesFreshAnalysis|AnalysisTraceFixtureParityWithLibopus)' -count=1`; `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`); full runnable sweep passed (`go list -e -f '{{if not .Error}}{{.ImportPath}}{{end}}' ./... | rg -v '/tmp_check$' | xargs go test -count=1`). Perf guard stayed green (`make bench-guard`). Bench-binary stash A/B (`go run ./examples/bench-encode -sample speech -iters 20 -warmup 3 -mode gopus -bitrate 64000 -complexity 10`) improved from baseline `best 277.177375ms`, `avg 281.044162ms` to candidate `best 273.911625ms`, `avg 278.773247ms` (~`1.18%` best, `0.81%` avg faster).
- 2026-03-04: Optimized ARM64 `pitchAutocorr5` kernel in `celt/pitch_autocorr_arm64.s` by unrolling SIMD inner accumulation from 4 to 8 elements per iteration, keeping 4/2/1 tail handling and preserving float32 accumulation order; also initialized inner pointers before tail paths to keep small-`fastN` behavior explicit. Quality/parity remained green: `go test ./celt -count=1`; `go test ./encoder -run 'Test(Analysis|RunAnalysis|TonalityAnalysis|UpdateOpusVADReusesFreshAnalysis|AnalysisTraceFixtureParityWithLibopus)' -count=1`; `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`); full runnable sweep passed (`go list -e -f '{{if not .Error}}{{.ImportPath}}{{end}}' ./... | rg -v '/tmp_check$' | xargs go test -count=1`). Perf guard stayed green (`make bench-guard`). Bench-binary stash A/B (`go run ./examples/bench-encode -sample speech -iters 20 -warmup 3 -mode gopus -bitrate 64000 -complexity 10`) improved from baseline `best 273.530834ms`, `avg 276.484366ms` to candidate `best 270.693291ms`, `avg 274.498254ms` (~`1.0%` best, `0.7%` avg faster).
- 2026-03-04: Optimized ARM64 prefilter dot-product kernels in `celt/prefilter_innerprod_arm64.s` by unrolling both `prefilterInnerProd` and `prefilterDualInnerProd` SIMD loops from 4 to 8 elements per iteration, while preserving accumulation order and float32 arithmetic semantics. Quality/parity remained green: `go test ./celt -count=1`; `go test ./encoder -run 'Test(Analysis|RunAnalysis|TonalityAnalysis|UpdateOpusVADReusesFreshAnalysis|AnalysisTraceFixtureParityWithLibopus)' -count=1`; `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`); full runnable sweep passed (`go list -e -f '{{if not .Error}}{{.ImportPath}}{{end}}' ./... | rg -v '/tmp_check$' | xargs go test -count=1`). Perf guard stayed green: `make bench-guard` passed (`0 allocs/op`). Bench-binary stash A/B showed consistent wins: `mode=gopus,iters=20,warmup=3` candidate `best 263.696541ms`, `avg 267.300729ms` vs baseline `best 266.359167ms`, `avg 269.458806ms` (~`1.0%` best, `0.8%` avg faster); `mode=both,iters=8,warmup=2` candidate gopus `best 274.832250ms`, `avg 277.854557ms` vs baseline gopus `best 276.766709ms`, `avg 283.887500ms`.
- 2026-03-04: Optimized MDCT pre/post twiddle hot loops in `celt/mdct_encode.go` by splitting inner loops into specialized paths (`mdctUseFMALikeMixEnabled` true/false and direct-`kissCpx` vs fallback `complex64`) so branch selection is hoisted out of per-bin iterations. Math/order is unchanged inside each selected path; this is a loop-structure optimization only. Quality remained green on focused parity checks: `go test ./celt -run 'Test(Transient|PrefilterPitchXcorr|RunPrefilterParityAgainstLibopusFixture|Tone|MDCT)' -count=1`; `go test ./encoder -run 'Test(Analysis|RunAnalysis|TonalityAnalysis|UpdateOpusVADReusesFreshAnalysis|AnalysisTraceFixtureParityWithLibopus)' -count=1`; `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`). Broad runnable sweep also passed (`go list -e -f '{{if not .Error}}{{.ImportPath}}{{end}}' ./... | rg -v '/tmp_check$' | xargs go test -count=1`). Perf guard stayed green: `make bench-guard` passed (`0 allocs/op`). Bench-binary A/B (stash-based, same host/session) showed consistent gopus gains: candidate (`go run ./examples/bench-encode -sample speech -iters 20 -warmup 3 -mode gopus -bitrate 64000 -complexity 10`) `best 269.492375ms`, `avg 273.872166ms` vs baseline `best 272.930583ms`, `avg 275.878118ms` (~`1.26%` best, `0.73%` avg faster). Shorter confirmation run (`iters 8`, `mode both`) also favored candidate (`best 275.243667ms`, `avg 278.077531ms`) over baseline (`best 277.689708ms`, `avg 279.835619ms`).
- 2026-03-04: Optimized `celt/mdct_encode.go` MDCT FFT staging by writing pre-twiddled values directly into bit-reversed `kissCpx` scratch and running `st.fftImpl()` in-place for supported FFT sizes, while keeping the prior `kissFFT32To` path as fallback for unsupported states. This removes intermediate `complex64` staging in the hot MDCT path without changing transform math order. Quality/parity stayed green: `go test ./celt -run 'Test(Transient|PrefilterPitchXcorr|RunPrefilterParityAgainstLibopusFixture|Tone|MDCT)' -count=1`; `go test ./encoder -run 'Test(Analysis|RunAnalysis|TonalityAnalysis|UpdateOpusVADReusesFreshAnalysis|AnalysisTraceFixtureParityWithLibopus)' -count=1`; `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`). Bench evidence via examples bench binary (as requested): candidate (`go run ./examples/bench-encode -sample speech -iters 8 -warmup 2 -mode gopus -bitrate 64000 -complexity 10`) measured `best 282.346709ms (175.29x)`, `avg 284.964474ms (173.68x)`; baseline (same command via stash A/B) measured `best 285.893458ms (173.12x)`, `avg 288.955969ms (171.28x)`; roughly `~1.3%` best and `~1.4%` average throughput improvement for gopus in this harness. Guardrails stayed green: `make bench-guard` passed (`0 allocs/op`); `make verify-production` still failed only on known local `tmp_check` cgo-disabled setup while all runnable packages passed.
- 2026-03-03: Optimized `celt/transient.go` by fusing pair-energy computation and forward masking decay into one loop in `transientAnalysisScratch`, and removed the now-unused transient pair-energy scratch (`transientX2`) from `celt/encoder.go` scratch sizing. This keeps the same transient detector math order while cutting one full buffer pass and reducing scratch traffic. Quality/parity stayed green: `go test ./celt -run 'Test(Transient|PrefilterPitchXcorr|RunPrefilterParityAgainstLibopusFixture|Tone)' -count=1`; `go test ./celt -count=1`; `go test ./encoder -run 'Test(Analysis|RunAnalysis|TonalityAnalysis|UpdateOpusVADReusesFreshAnalysis|AnalysisTraceFixtureParityWithLibopus)' -count=1`; `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`). Perf evidence: controlled A/B root microbench (`GOMAXPROCS=1 go test ./ -bench 'BenchmarkEncoderEncode$|BenchmarkEncoderEncodeInt16$' -benchmem -run '^$' -count=8 -benchtime=2s -cpu=1`) improved current vs stashed baseline from `~52.2-54.0 us/op` to `~51.5-53.5 us/op` for `BenchmarkEncoderEncode`, and from `~53.5-54.0 us/op` to `~51.9-52.8 us/op` for `BenchmarkEncoderEncodeInt16` (about ~1.7-2.8% win depending on run). `make bench-guard` passed with `0 allocs/op`; `make verify-production` remained blocked only by known local `tmp_check` cgo-disabled setup while all runnable packages passed. End-to-end harness on speech sample stayed behind C but improved from earlier session baseline (`go run ./examples/bench-encode -sample speech -iters 2 -warmup 1 -mode both -bitrate 64000 -complexity 10`: gopus best `186.69x` realtime vs ffmpeg/libopus `214.85x`).
- 2026-03-02: Added two more encoder hot-path optimizations with parity preserved. (1) `celt/transient.go` transient detector now keeps HP-filter scratch (`tmp`, `energy`) in `float32` end-to-end and computes pair energies directly from float32 (`transientEnergyPairsF32`), removing repeated float64<->float32 conversions in `transientAnalysisScratch`; corresponding scratch storage in `celt/encoder.go` was switched to float32. (2) `encoder/analysis_mlp.go` now precomputes float32 copies of analysis MLP int8 weights once at init (`initAnalysisMLPWeightCaches`) and uses `gemmAccumF32` in dense/GRU inference instead of per-multiply int8->float32 conversions. Quality/parity checks remained green: `go test ./celt -run 'Test(Transient|PrefilterPitchXcorr|RunPrefilterParityAgainstLibopusFixture|Tone)' -count=1`; `go test ./celt -count=1`; `go test ./encoder -run 'Test(Analysis|RunAnalysis|TonalityAnalysis|UpdateOpusVADReusesFreshAnalysis|AnalysisTraceFixtureParityWithLibopus)' -count=1`; `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`). Perf evidence: root microbench (`go test . -run '^$' -bench 'BenchmarkEncoderEncode$|BenchmarkEncoderEncodeInt16$' -benchmem -benchtime=2s -count=5`) moved `BenchmarkEncoderEncodeInt16` from prior ~`55.1k ns/op` cluster down to ~`50.3-51.3k ns/op` best cluster (float32 path also improved with best samples near `53.1-53.5k ns/op`); `make bench-guard` passed with encoder samples around `~50.9-54.7k ns/op` and `0 allocs/op`. CPU profile confirmation (`go test . -run '^$' -bench '^BenchmarkEncoderEncode$' -benchtime=6s -cpuprofile`): `celt.(*Encoder).transientAnalysisScratch` flat share dropped from ~`7.3%` pre-change to ~`5.2%` post-change. End-to-end reference harness remains behind C but improved relative to earlier baseline in this session (`go run ./examples/bench-encode -sample speech -iters 2 -warmup 1 -mode both -bitrate 64000 -complexity 10`: gopus around `~174-175x` realtime vs ffmpeg/libopus around `~199-203x`, host-noise dependent).
- 2026-03-02: Improved encoder tonality-analysis hot path again in `encoder/analysis.go` while preserving parity. Changes: (1) cached per-band `log(bandE)` once and reused it for both `logE[]` and `bandLog2[]`, (2) cached per-frame `sqrt(E)` into `SqrtE` and reused it in stationarity accumulation to avoid repeated square roots over history frames, and (3) removed a redundant per-band raw-energy rescan by accumulating `bandERaw` during the primary band pass and reusing it in the later bandwidth-mask pass. Added direct A/B microbenchmark guard `encoder/analysis_band_energy_bench_test.go` to compare legacy vs current section behavior in isolation. Performance evidence (`GOMAXPROCS=1 go test ./encoder -run '^$' -bench 'BenchmarkAnalysisBandEnergy(Legacy|Current)$' -benchmem -benchtime=2s -count=6 -cpu=1`): legacy `~528.7-539.5 ns/op`, current `~400.1-402.5 ns/op` (~24-26% faster for the optimized section, `0 allocs/op`). End-to-end guard stayed healthy (`make bench-guard`: `BenchmarkEncoderEncode ~56051-56436 ns/op` sample cluster, `BenchmarkEncoderEncodeInt16` best samples around `~56247-57880 ns/op`, `0 allocs/op`). Quality/parity checks remained green: `go test ./encoder -run 'Test(Analysis|RunAnalysis|TonalityAnalysis|UpdateOpusVADReusesFreshAnalysis|AnalysisTraceFixtureParityWithLibopus)' -count=1`; `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`). Broad gate status unchanged: `make verify-production` still fails only on known local `tmp_check` cgo-disabled blocker while all runnable packages pass.
- 2026-03-02: Added another CELT prefilter/pitch hot-path optimization in `celt/prefilter.go` with parity preserved. Changes: (1) specialized `pitchDownsample()` fast path for the dominant `factor=2` case (mono/stereo) using stride-2 index progression, (2) reduced decimation-loop index math in `pitchSearch()` by using running indices, and (3) skipped redundant `prefilterMem`/overlap re-rounding in the default float32 prefilter path while keeping explicit rounding in debug/alt-precision modes (`tmpSkipPrefInputRoundEnabled` or `tmpPrefilterF64Enabled`). Added focused A/B benchmark coverage in `celt/pitch_downsample_bench_test.go` (`Current` vs `Legacy`). Performance evidence (arm64, `go test ./celt -run '^$' -bench 'BenchmarkPitchDownsample(Current|Legacy)(Mono|Stereo)$' -benchmem -benchtime=2s -count=5`): mono path roughly neutral/slightly better (`~1920-1994 ns/op` vs legacy `~1931-2010 ns/op`), stereo path improved clearly (`~2252-2316 ns/op` vs legacy `~2471-2519 ns/op`, about `8-10%` faster). Quality/parity checks stayed green: `go test ./celt -run 'Test(PrefilterPitchXcorr|RunPrefilterParityAgainstLibopusFixture|TransientAnalysis)' -count=1`; `go test ./celt -count=1`; `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`); full runnable package sweep (`go list -e -f '{{if not .Error}}{{.ImportPath}}{{end}}' ./... | rg -v '/tmp_check$' | xargs go test -count=1`) passed; `make bench-guard` passed.
- 2026-03-02: Optimized `pitchSearch()` fine-stage candidate evaluation in `celt/prefilter.go` without changing libopus decision logic. Replaced full-range `abs()`-gated scan with explicit `±2` candidate windows around both coarse winners, kept zero-fill semantics via `clear(xcorr[:halfPitch])`, and retained identical downstream `findBestPitch`/interpolation flow. Added focused hotspot benchmark coverage in `celt/pitch_search_bench_test.go` with both current and legacy implementations for direct A/B measurement. Measured on arm64 (`go test ./celt -run '^$' -bench 'BenchmarkPitchSearch(Current|Legacy)$' -benchmem -benchtime=2s -count=5`): current `~3764-3828 ns/op` vs legacy `~4088-4159 ns/op` (~8% faster). Quality/regression checks stayed green: `go test ./celt -run 'Test(PrefilterPitchXcorr|RunPrefilterParityAgainstLibopusFixture|TransientAnalysis)' -count=1`; `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`). Perf gate stayed green: `make bench-guard` passed (`BenchmarkEncoderEncode 57154 ns/op`, `BenchmarkEncoderEncodeInt16 56874 ns/op`, `0 allocs/op`). Broad gate status unchanged: `make verify-production` still fails only on known local `tmp_check` cgo-disabled blocker while non-`tmp_check` packages pass.
- 2026-03-02: Added another CELT prefilter hot-path optimization in `celt/prefilter.go` after profiling `runPrefilter` as the highest cumulative cost (`~24.8%`). Implemented mono fast-path gather/scatter using direct `copy` (instead of interleaved per-sample loops) and avoided redundant full-buffer input rounding by rounding only the newly appended frame region when persisted prefilter history is already float32-quantized (`tmpSkipPrefMemRoundEnabled==false`). libopus parity stayed exact on dedicated fixture guard: `go test ./celt -run 'TestRunPrefilterParityAgainstLibopusFixture' -count=1 -v` (`cases=300`, all mismatch counters `0`, `maxGainDiff=0.000000`). Additional focused checks stayed green: `go test ./celt -run 'Test(TransientAnalysis|PrefilterPitchXcorr)' -count=1` and `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`). Conservative perf evidence from guard-style runs shows improvement: `make bench-guard` encoder rows moved from roughly `~55.2 us/op` to `~54.6 us/op` for `BenchmarkEncoderEncode`, and from `~55.8 us/op` to `~54.8 us/op` for `BenchmarkEncoderEncodeInt16` (with normal run-to-run variance outside guard). `make bench-guard` passed; `make verify-production` stayed blocked only by the known local `tmp_check` cgo-disabled setup while non-`tmp_check` packages passed. End-to-end harness remained behind C but improved relative to the starting point of this session: gopus now observed around `~187x` realtime vs ffmpeg/libopus around `~213x` on the speech sample.
- 2026-03-02: Continued encoder performance hardening in `celt/transient.go` with no quality regression. In `transientAnalysisScratch`, removed per-channel copy into `channelSamples` and consumed mono/stereo samples directly from `pcm` in the HP-filter loop (same arithmetic/order as before), reducing hot-path memory traffic and copy overhead. Validation stayed green: `go test ./celt -run 'Test(Transient|Tone|PatchTransientDecision)' -count=1`, `go test ./celt -run 'TestTransientAnalysis' -count=1 -v`, and `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`). Bench evidence on arm64 (`cpu=1`, same slice): `BenchmarkEncoderEncode` improved from ~`55.7-55.9 us/op` to ~`54.6-55.1 us/op` (additional ~2%), `BenchmarkEncoderEncodeInt16` improved from ~`56.4-56.6 us/op` to ~`55.2-55.7 us/op` (additional ~2%), and `BenchmarkEncoderEncode_VoIP` improved from ~`51.2-51.6 us/op` to ~`50.2-50.3 us/op`. Broad gates: `make bench-guard` passed; `make verify-production` remained locally blocked only by known `tmp_check` cgo-disabled setup while non-`tmp_check` packages passed. End-to-end C reference harness (`go run ./examples/bench-encode -sample speech -iters 2 -warmup 1 -mode both -bitrate 64000 -complexity 10`) improved from ~`169x` to ~`184x` realtime for gopus, while ffmpeg/libopus was ~`209x`; gap narrowed but C is still ahead.
- 2026-03-02: Started encoder performance hardening without quality tradeoff in `encoder/analysis.go`. Hoisted tonality FFT/phase temporaries (`in/out`, `tonality`, `tonality2`, `noisiness`) into persistent `TonalityAnalysisState` scratch to reduce per-call stack pressure, and reused precomputed raw FFT bin energies for the later bandwidth pass instead of recomputing from FFT output. Quality/parity checks stayed green: `go test ./encoder -run 'Test(Analysis|RunAnalysis|TonalityAnalysis|UpdateOpusVADReusesFreshAnalysis|AnalysisTraceFixtureParityWithLibopus)' -count=1` and `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v` (`23 passed, 0 failed`). Bench evidence on arm64 (`cpu=1`, same benchmark slice): `BenchmarkEncoderEncode` improved from ~`56.6-57.4 us/op` to ~`55.7-55.9 us/op` (~2% better), and `BenchmarkEncoderEncodeInt16` from ~`56.6-57.8 us/op` to ~`56.4-56.6 us/op` (~1% better), still `0 allocs/op`. CPU profile moved in the expected direction: `TonalityAnalysisState.tonalityAnalysis` cum share dropped (`17.43% -> 14.21%`) and `runtime.morestack` dropped (`3.49% -> 2.19%`). Broad gates: `make bench-guard` passed; `make verify-production` remained locally blocked only by known `tmp_check` cgo-disabled setup while non-`tmp_check` packages passed. End-to-end reference harness result (`go run ./examples/bench-encode -sample speech -iters 2 -warmup 1 -mode both -bitrate 64000 -complexity 10`): gopus ~`169x` realtime vs ffmpeg/libopus ~`204x` realtime, so C parity-quality is preserved but performance is still behind and needs more loops.
- 2026-03-01: Tightened the next cross-arch ratchet lanes in `testvectors/testdata/encoder_compliance_variants_ratchet_baseline*.json`. Updated `SILK-WB-20ms-mono-32k|am_multisine_v1` and `SILK-WB-40ms-mono-32k|am_multisine_v1` to `min_gap_db=-0.02` on both baselines; tightened `HYBRID-SWB-40ms-mono-48k|impulse_train_v1` to default `-0.05` / amd64 `-0.12`; tightened `SILK-WB-40ms-mono-32k|impulse_train_v1` to default `-0.03` / amd64 `-0.11`; tightened `SILK-NB-40ms-mono-16k|am_multisine_v1` to default `-0.03` / amd64 `-0.08`. Focused repeated probes were deterministic across 3 runs per arch: arm64 `+0.06/+0.03/-0.00 dB` and amd64 `-0.10/-0.09/-0.06 dB` for the three targeted weak lanes (`HYBRID-SWB-40ms impulse`, `SILK-WB-40ms impulse`, `SILK-NB-40ms am`). Validation passed: `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderVariantProfileParityAgainstLibopusFixture -count=1`, `GOARCH=amd64 GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderVariantProfileParityAgainstLibopusFixture -count=1`, `GOPUS_TEST_TIER=exhaustive go test ./testvectors -run TestEncoderVariantProfileProvenanceAudit -count=1 -v`, `GOARCH=amd64 GOPUS_TEST_TIER=exhaustive go test ./testvectors -run TestEncoderVariantProfileProvenanceAudit -count=1 -v`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v`, `GOARCH=amd64 GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v`, and `make bench-guard`. Broad gate `make verify-production` remained locally blocked only by expected `tmp_check` cgo-disabled setup while non-`tmp_check` packages passed.
- 2026-03-01: Tightened the next weakest amd64 SILK-WB-60ms ratchet floors in `testvectors/testdata/encoder_compliance_variants_ratchet_baseline_amd64.json`: `SILK-WB-60ms-mono-32k|am_multisine_v1` and `|impulse_train_v1` `min_gap_db -0.05 -> -0.03` (default baseline unchanged at `-0.03` for `am_multisine` and `-0.04` for `impulse_train`). Focused repeated subtest probes (expected parent-level `ratchet baseline coverage mismatch` on subtest-only invocation): arm64 `impulse_train` stayed `gap=-0.04 dB` across 5 runs; amd64 `impulse_train` stayed `gap=-0.00 dB` across 5 runs; arm64/amd64 `am_multisine` stayed `gap=0.00 dB` across 5 runs. Validation passed after tightening: `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderVariantProfileParityAgainstLibopusFixture -count=1`, `GOARCH=amd64 GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderVariantProfileParityAgainstLibopusFixture -count=1`, `GOPUS_TEST_TIER=exhaustive go test ./testvectors -run TestEncoderVariantProfileProvenanceAudit -count=1 -v`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v`, and `make bench-guard`. Broad gate `make verify-production` remained blocked only by the known local `tmp_check` cgo-disabled setup while non-`tmp_check` packages passed.
- 2026-03-01: Expanded libopus-backed ambisonics parity coverage in `multistream/libopus_test.go` to include higher-order families beyond FOA/SOA. Added Family-2 TOA lanes (`16ch`, `18ch`) and Family-3 TOA/4th/5th-order lanes (`16ch`, `18ch`, `25ch`, `27ch`, `36ch`, `38ch`) in `TestLibopus_AmbisonicsFamily{2,3}Matrix`. Focused validation passed with libopus decode agreement and stable energy ratios across all new cases: `go test ./multistream -run 'TestLibopus_AmbisonicsFamily(2|3)Matrix' -count=1 -v` and `go test ./multistream -count=1`. Broad-gate evidence: `make verify-production` failed only on the known local `tmp_check` cgo-disabled blocker (`C source files not allowed when not using cgo or SWIG`) while non-`tmp_check` packages passed, and `make bench-guard` passed within configured limits. No missing libopus family-3 projection behavior was exposed by the expanded matrix.
- 2026-03-01: Tightened the next amd64 SILK ratchet slack lane in `testvectors/testdata/encoder_compliance_variants_ratchet_baseline_amd64.json`: `SILK-WB-20ms-mono-32k|am_multisine_v1` floor `min_gap_db -0.10 -> -0.05` (default baseline remained `-0.03`). Focused repeated subtest probes were stable on both arches at `gap=-0.00 dB` (`TestEncoderVariantProfileParityAgainstLibopusFixture/cases/SILK-WB-20ms-mono-32k-am_multisine_v1`; expected parent-level `ratchet baseline coverage mismatch` on subtest-only invocation). Validation passed with tightened floor: `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderVariantProfileParityAgainstLibopusFixture -count=1`, `GOARCH=amd64 GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderVariantProfileParityAgainstLibopusFixture -count=1`, `GOPUS_TEST_TIER=exhaustive go test ./testvectors -run TestEncoderVariantProfileProvenanceAudit -count=1 -v`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v`, and `make bench-guard`. `make verify-production` remained locally blocked only by known `tmp_check` cgo-disabled setup while non-`tmp_check` packages (including `testvectors`) passed.
- 2026-03-01: Tightened the next SILK ratchet slack lanes in `testvectors/testdata/encoder_compliance_variants_ratchet_baseline*.json`: `SILK-WB-40ms-mono-32k|am_multisine_v1` default floor `-0.04 -> -0.03`, amd64 floor `-0.08 -> -0.05`, and `SILK-WB-60ms-mono-32k|impulse_train_v1` amd64 floor `-0.08 -> -0.05` (default remained `-0.04` after repeated arm64 probes stayed at `gap=-0.04 dB`). Focused repeated subtest probes: arm64 `SILK-WB-40ms am` stayed at `gap=-0.00 dB` and amd64 at `gap=0.00 dB`; arm64 `SILK-WB-60ms impulse` stayed at `gap=-0.04 dB` while amd64 stayed at `gap=-0.00 dB` (`TestEncoderVariantProfileParityAgainstLibopusFixture/cases/...`; expected parent-level `ratchet baseline coverage mismatch` on subtest-only invocation). Validation passed with tightened floors: `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderVariantProfileParityAgainstLibopusFixture -count=1`, `GOARCH=amd64 GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderVariantProfileParityAgainstLibopusFixture -count=1`, `GOPUS_TEST_TIER=exhaustive go test ./testvectors -run TestEncoderVariantProfileProvenanceAudit -count=1 -v`, `GOPUS_TEST_TIER=parity go test ./testvectors -run TestEncoderComplianceSummary -count=1 -v`, and `make bench-guard`. `make verify-production` remained locally blocked only by known `tmp_check` cgo-disabled setup while non-`tmp_check` packages (including `testvectors`) passed.
- 2026-03-01: Merged PR #261 (`test(testvectors): tighten silk wb 60ms am ratchet floors`) with full green CI; tightened `SILK-WB-60ms-mono-32k|am_multisine_v1` floors to default `-0.03`, amd64 `-0.05`.
- 2026-03-01: Merged PR #260 (`test(testvectors): tighten silk wb 60ms impulse ratchet floors`) with full green CI; tightened `SILK-WB-60ms-mono-32k|impulse_train_v1` floors to default `-0.04`, amd64 `-0.08`.
- 2026-03-01: Merged PR #259 (`test(testvectors): tighten final celt compliance override floor`) with full green CI; tightened CELT override `0.20 -> 0.191`.
- 2026-02-28: Compliance summary stabilized at `23 passed, 0 failed` after source-aligned cadence and row-level no-negative override calibration.
- 2026-02-28: Compliance reference-Q path aligned to libopus-only decode order (direct helper first, `opusdec` fallback, no internal fallback for fixture calibration).
- 2026-02-28: Ported hybrid held-frame SILK/Hybrid->CELT transition redundancy cadence and closed `HYBRID-SWB-20ms-mono-48k|am_multisine_v1` negative residual lane.
